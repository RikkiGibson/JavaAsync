package com.microsoft.azure.storage.pipeline;


//import org.apache.log4j.Logger;
//import org.apache.log4j.nt.NTEventLogAppender;
//import org.apache.log4j.spi.LoggingEvent;
//
//import static org.apache.log4j.Level.*;

// For internal use only so customer cannot modify the logger
final class DefaultWindowsEventLogWriter {

    static void Initialize() {
//        NTEventLogAppender ntEventLogAppender = new NTEventLogAppender();
//        ntEventLogAppender.setThreshold(ERROR);
//        Logger ourLog = Logger.getLogger(DefaultWindowsEventLogWriter.class);
//        //ourLog.
//        //Logger outLogger =Logger.
//        //ntEventLogAppender.append(new LoggingEvent(){});
//        Logger.getRootLogger().addAppender(ntEventLogAppender); // change to new logger
    }
}
